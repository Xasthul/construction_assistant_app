import 'package:flutter/material.dart';

class ProjectSettingsRenameComponent extends StatelessWidget {
  const ProjectSettingsRenameComponent({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
